import React from 'react';
import{
  SafeAreaView,
  View,
  ScrollView
} from 'react-native'

function App() : JSX.Element {
  const name = 'UTP-Services'
  return (
    <SafeAreaView>
      <ScrollView>
      <View>Checking branch pushing and observing the pr request</View>
      </ScrollView>
    </SafeAreaView>
  );
}
export default App;